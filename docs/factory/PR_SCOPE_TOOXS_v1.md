# PR scope guard

This migration PR is documentation/factory-configuration only. It must not modify runtime code, generated API types, feature flags, Cloudflare configuration, CI/deploy workflows, secrets, or production behavior.
