import { OpeningBalanceView } from "@/components/onboarding/opening-balance-view";

/* `/onboarding/saldo-apertura` — paso 6 (post-auth). Gating del flag por el layout. */
export default function SaldoAperturaPage() {
  return <OpeningBalanceView />;
}
