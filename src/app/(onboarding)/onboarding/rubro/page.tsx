import { IndustryView } from "@/components/onboarding/industry-view";

/* `/onboarding/rubro` — paso 5 (post-auth). Gating del flag por el layout. */
export default function RubroPage() {
  return <IndustryView />;
}
